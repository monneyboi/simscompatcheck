[project]
name = "simscompatcheck"
version = "0.1.0"
description = "Sims 1 compatibility checker - find the best conversation partners"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115",
    "uvicorn>=0.34",
]
